# URL base da API do GitHub (opcional)
VITE_API_URL=https://api.github.com

# Token pessoal do GitHub (opcional, recomendado para evitar rate limit 403)
# Crie em: https://github.com/settings/tokens — escopo público, sem permissões extras
VITE_GITHUB_TOKEN=
